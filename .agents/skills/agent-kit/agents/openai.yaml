interface:
  display_name: "Agent Kit"
  short_description: "Repo-specific patterns and workflows for agent-kit"
  default_prompt: "Use the agent-kit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true